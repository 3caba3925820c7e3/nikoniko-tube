# NicoStream Railway
FastAPI + yt-dlp NicoNico search and playback prototype.

Connect the GitHub repository to Railway. No environment variables are required.
Railway/Nixpacks uses requirements.txt and the included start command.

Local:
pip install -r requirements.txt
uvicorn app:app --reload
